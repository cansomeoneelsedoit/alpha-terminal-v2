{
  "name": "alpha-terminal-v2",
  "version": "2.0.0",
  "private": true,
  "description": "Alpha Terminal V2 — Structured Decision Engine + Trading Platform with Bitget Live Data",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --port ${PORT:-3000} --host 0.0.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.4.0"
  }
}
